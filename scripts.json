    "build:api": "esbuild api/index.js --bundle --platform=node --outfile=api/dist/index.js",
    "build:sync": "esbuild spotify-sync/index.js --bundle --platform=node --outfile=spotify-sync/dist/index.js",
    "dev:sync": "cd spotify-sync && node --env-file=../.env index.js",
    "dev:auth-setup": "cd spotify-sync/auth-setup && node --env-file=../../.env index.js",
    "dev:api": "cd api && node --env-file=../.env index.js"
    // app.all('/{*any}', (req, res, next) => {
    //   throw new AppError(404, `Can't find ${req.originalUrl} on this server`);
    // });
    // app.all("/*", (req, res) => res.send(`path seen: ${req.path}`));